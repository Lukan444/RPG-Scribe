/**
 * Application-wide constants
 */

/**
 * Default world ID to use when no specific world is selected
 * This is used for global entity pages that should display entities across all worlds
 */
export const DEFAULT_WORLD_ID = 'global';

/**
 * Default campaign ID to use when no specific campaign is selected
 * This is used for global entity pages that should display entities across all campaigns
 */
export const DEFAULT_CAMPAIGN_ID = 'global';
